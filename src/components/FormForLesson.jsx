"use client";
import { db } from "@/utils/utilities";
import { useState } from "react";


export default async function formForIndividualLesson({}) {
const [checkResponse, setCheckResponse] = useState(false)


  return (
  
  <div mode="modal">
   </div>
  )
}
